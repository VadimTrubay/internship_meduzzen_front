export const selectMyNotifications = (state) => state.notifications.items;

export const selectLoading = (state) => state.notifications.loading;

export const selectError = (state) => state.notifications.error;
